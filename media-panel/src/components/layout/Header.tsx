export default function Header() {
  return (
    <header className="app-header">
      <div>
        <h1>Media Panel</h1>
        <p>Run broadcasts, announcements, and sermon workflows.</p>
      </div>
      <button className="secondary-button" type="button">Logout</button>
    </header>
  )
}
